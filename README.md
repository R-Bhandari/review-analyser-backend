# Review Analyser - Backend

Backend for the Review Analyser SaaS platform built using Node.js, Express, and MongoDB.

## Setup Instructions
1. Clone the repo
2. Run `npm install`
3. Add your `.env` file
4. Run `npm run dev`
